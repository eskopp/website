<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Tags/Cadddy on Erik Skopp</title><link>https://backup.erik-skopp.de/tags/cadddy/</link><description>Recent content in Tags/Cadddy on Erik Skopp</description><generator>Hugo</generator><language>de</language><copyright>Copyright © 2023-2025 Erik Skopp. All Rights Reserved.</copyright><lastBuildDate>Fri, 07 Feb 2025 06:34:17 +0100</lastBuildDate><atom:link href="https://backup.erik-skopp.de/tags/cadddy/index.xml" rel="self" type="application/rss+xml"/><item><title>Nextcloud auf Alpine mit Caddy</title><link>https://backup.erik-skopp.de/nextcloud-auf-alpine-mit-caddy/</link><pubDate>Fri, 07 Feb 2025 06:34:17 +0100</pubDate><guid>https://backup.erik-skopp.de/nextcloud-auf-alpine-mit-caddy/</guid><description><![CDATA[<h1 id="anlegen-einer-nextcloud-instanz-auf-einem-alpine-server-mit-caddy" data-numberify>Anlegen einer Nextcloud Instanz auf einem Alpine Server mit Caddy<a class="anchor ms-1" href="#anlegen-einer-nextcloud-instanz-auf-einem-alpine-server-mit-caddy"></a></h1>

<h2 id="dns-konfiguration-für-die-subdomain" data-numberify>DNS-Konfiguration für die Subdomain<a class="anchor ms-1" href="#dns-konfiguration-für-die-subdomain"></a></h2>
<p>Um den Server über eine Domain oder Subdomain erreichbar zu machen, ist die Erstellung eines entsprechenden DNS-Records erforderlich. In diesem Fall wird die Domain <code>erik-skopp.de</code> genutzt und eine Third-Level-Domain mit dem Namen <code>nextcloud</code> angelegt. Daraus ergibt sich die finale Subdomain: <code>nextcloud.erik-skopp.de</code>.</p>

<h3 id="anlegen-der-records" data-numberify>Anlegen der Records<a class="anchor ms-1" href="#anlegen-der-records"></a></h3>
<p>Diese Subdomain dient ausschließlich zu Testzwecken. Nach Abschluss dieses Berichts werden sowohl der Webserver als auch die Nextcloud-Installation sowie die zugehörigen DNS-Einträge entfernt.</p>]]></description><content:encoded><![CDATA[
<h1 id="anlegen-einer-nextcloud-instanz-auf-einem-alpine-server-mit-caddy" data-numberify>Anlegen einer Nextcloud Instanz auf einem Alpine Server mit Caddy<a class="anchor ms-1" href="#anlegen-einer-nextcloud-instanz-auf-einem-alpine-server-mit-caddy"></a></h1>

<h2 id="dns-konfiguration-für-die-subdomain" data-numberify>DNS-Konfiguration für die Subdomain<a class="anchor ms-1" href="#dns-konfiguration-für-die-subdomain"></a></h2>
<p>Um den Server über eine Domain oder Subdomain erreichbar zu machen, ist die Erstellung eines entsprechenden DNS-Records erforderlich. In diesem Fall wird die Domain <code>erik-skopp.de</code> genutzt und eine Third-Level-Domain mit dem Namen <code>nextcloud</code> angelegt. Daraus ergibt sich die finale Subdomain: <code>nextcloud.erik-skopp.de</code>.</p>

<h3 id="anlegen-der-records" data-numberify>Anlegen der Records<a class="anchor ms-1" href="#anlegen-der-records"></a></h3>
<p>Diese Subdomain dient ausschließlich zu Testzwecken. Nach Abschluss dieses Berichts werden sowohl der Webserver als auch die Nextcloud-Installation sowie die zugehörigen DNS-Einträge entfernt.</p>
<p>Zur Vereinfachung werden hier nur zwei Domains verwendet:</p>
<ul>
<li>Der <code>A-Record</code> weist die Domain der IPv4-Adresse <code>152.53.120.57</code> zu.</li>
<li>Der <code>AAAA-Record</code> löst <code>nextcloud.erik-skopp.de</code> auf IPv6 auf, nämlich auf <code>2a0a:4cc0:c0:4540::1</code>.</li>
</ul>
<p>Für dieses Projekt nutze ich einen <code>Netcup-Server</code>, der einen <code>/64-Adresspool</code> bereitstellt. Konkret sind dies:</p>
<ul>
<li><code>Link-Local-Adresse:</code> <code>fe80::a8cb:d0ff:fe50:ab47/10</code> (nicht von außen erreichbar)</li>
<li><code>Global Unicast-Subnetz:</code> <code>2a0a:4cc0:c0:4540::/64</code> (öffentlich routbar)</li>
</ul>
<p>Da die <code>Link-Local-Adresse (</code>fe80::&hellip;/10<code>) nicht extern erreichbar</code> ist, bleibt nur die <code>routbare Global Unicast-Adresse</code>.
Aus dem <code>/64</code>-Adresspool habe ich die <code>:1</code> als Host-Adresse gewählt – dies geschah aus rein praktischen Gründen und hat keinen Einfluss auf den weiteren Ablauf.</p>
<p><picture><img class="img-fluid " alt="DNS Eintrag" src="/nextcloud-auf-alpine-mit-caddy/dns_eintraege.webp" loading="lazy" width="837" height="100" />
</picture>

</p>

<h3 id="testen-der-records" data-numberify>Testen der Records<a class="anchor ms-1" href="#testen-der-records"></a></h3>

<h3 id="dns-checker" data-numberify>DNS-Checker<a class="anchor ms-1" href="#dns-checker"></a></h3>
<p>Die Überprüfung der DNS-Einträge erfolgt auf zwei Wegen. Eine Möglichkeit bietet die externe Website <a href="https://dnschecker.org/#A/nextcloud.erik-skopp.de" target="_blank" rel="noopener noreferrer">DNSChecker<i class="fas fa-external-link-square-alt ms-1"></i></a>. Dort kann die gewünschte URL eingegeben und der zu überprüfende DNS-Record ausgewählt werden. Die Ergebnisse werden übersichtlich dargestellt und zeigen, in welchen Regionen der Eintrag bereits propagiert wurde und wo er noch aussteht.</p>
<p><picture><img class="img-fluid " alt="DNS Checker" src="/nextcloud-auf-alpine-mit-caddy/dnschecker.webp" loading="lazy" width="1580" height="768" />
</picture>

</p>

<h4 id="dig" data-numberify>DIG<a class="anchor ms-1" href="#dig"></a></h4>
<blockquote>
<p>dig (Domain Information Groper) ist ein leistungsstarkes Kommandozeilen-Tool zum Abfragen von DNS-Informationen. Es wird häufig von Netzwerkadministratoren und Entwicklern [&hellip;] genutzt, um DNS-Records von Domains zu analysieren. (Quelle: verschiedene)<sup id="fnref:1"><a href="#fn:1" class="footnote-ref" role="doc-noteref">1</a></sup></p></blockquote>
<p>Wir nutzen dig um mit einem CLI Tool zu prüfen ob die DNS Einträge für die Nextcloud Domain vorhanden sind. Auf Gründen der einfachheit werde ich hier nur auf den <code>IPv4</code> bzw <code>A-Record</code> eingehen, da nur dieser hier relevant ist. Für den anderen Record bedarf es keine Änderung. Auf dem Server werden beide in der Caddy Config abgefangen.</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span><span class="lnt">19
</span><span class="lnt">20
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">&gt; dig nextcloud.erik-skopp.de
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">;</span> &lt;&lt;&gt;&gt; DiG 9.18.30-0xxx &lt;&lt;&gt;&gt; nextcloud.erik-skopp.de
</span></span><span class="line"><span class="cl"><span class="p">;;</span> global options: +cmd
</span></span><span class="line"><span class="cl"><span class="p">;;</span> Got answer:
</span></span><span class="line"><span class="cl"><span class="p">;;</span> -&gt;&gt;HEADER<span class="s">&lt;&lt;- opcode: QUERY, status: NOERROR, id: 8998
</span></span></span><span class="line"><span class="cl"><span class="s">;; flags: qr rd ra; QUERY: 1, ANSWER: 1, AUTHORITY: 0, ADDITIONAL: 1
</span></span></span><span class="line"><span class="cl"><span class="s">
</span></span></span><span class="line"><span class="cl"><span class="s">;; OPT PSEUDOSECTION:
</span></span></span><span class="line"><span class="cl"><span class="s">; EDNS: version: 0, flags:; udp: 512
</span></span></span><span class="line"><span class="cl"><span class="s">;; QUESTION SECTION:
</span></span></span><span class="line"><span class="cl"><span class="s">;nextcloud.erik-skop</span>p.de.       IN      A
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">;;</span> ANSWER SECTION:
</span></span><span class="line"><span class="cl">nextcloud.erik-skopp.de. <span class="m">182</span>    IN      A       152.53.120.57
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">;;</span> Query time: <span class="m">49</span> msec
</span></span><span class="line"><span class="cl"><span class="p">;;</span> SERVER: 10.255.255.254#53<span class="o">(</span>10.255.255.254<span class="o">)</span> <span class="o">(</span>UDP<span class="o">)</span>
</span></span><span class="line"><span class="cl"><span class="p">;;</span> WHEN: Fri Feb <span class="m">07</span> 10:17:31 CET <span class="m">2025</span>
</span></span><span class="line"><span class="cl"><span class="p">;;</span> MSG SIZE  rcvd: <span class="m">68</span>
</span></span></code></pre></td></tr></table>
</div>
</div><p>In der Antwortsektion ist zu erkennen, dass der Server den A-Record erfolgreich aufgelöst hat. Daher können wir uns nun der Serverkonfiguration widmen.</p>

<h2 id="einrichten-des-servers" data-numberify>Einrichten des Servers<a class="anchor ms-1" href="#einrichten-des-servers"></a></h2>

<h3 id="grundgedanke" data-numberify>Grundgedanke<a class="anchor ms-1" href="#grundgedanke"></a></h3>
<p>In den meisten Linux-Distributionen werden Websites standardmäßig im Verzeichnis <code>/var/www</code> abgelegt. Daher erstellen wir dort den Ordner <code>nextcloud</code>, der als Hauptverzeichnis für Nextcloud dient. Die Nutzerdaten hingegen werden in <code>/home/data/</code> gespeichert. Diese Trennung ist essenziell, da sie die Verwaltung von Zugriffsrechten erleichtert und die Struktur übersichtlicher hält.</p>
<p>Anschließend konfigurieren wir Caddy so, dass es die Index-Dateien aus dem Verzeichnis <code>/var/www/nextcloud</code> lädt. Da Nextcloud PHP erfordert, verwenden wir die aktuelle Version 8.3, die über den <code>PHP FastCGI Socket</code> eingebunden wird. PHP FastCGI (Fast Common Gateway Interface) ist eine optimierte Methode zur Kommunikation zwischen einem Webserver und der PHP-Interpreter-Instanz. Im Gegensatz zum klassischen CGI, bei dem für jede Anfrage ein neuer PHP-Prozess gestartet wird, hält FastCGI persistente Prozesse bereit, die mehrere Anfragen effizient verarbeiten können. Dies reduziert den Overhead und verbessert die Leistung von PHP-Anwendungen erheblich.</p>
<p>Ein <code>sock</code> (Socket) ist eine Datei, die als Kommunikationsschnittstelle zwischen zwei Prozessen auf demselben System dient. In diesem Fall ermöglicht ein Unix-Domain-Socket die direkte und schnelle Kommunikation zwischen dem Webserver (Caddy) und der PHP-FPM-Instanz. Der Webserver übergibt Anfragen an diesen Socket, woraufhin PHP-FPM die Anfragen verarbeitet und die Antworten zurückliefert.</p>
<p>Als Datenbank setzen wir MariaDB ein, um die erforderlichen Daten für Nextcloud zu verwalten.</p>
<p>Mit dieser Konfiguration ist die grundlegende Einrichtung abgeschlossen.</p>

<h3 id="grundlagen-von-alpine" data-numberify>Grundlagen von Alpine<a class="anchor ms-1" href="#grundlagen-von-alpine"></a></h3>
<p>In diesem Projekt verwenden wir Alpine Linux in der Version <code>3.21.3</code>, die über die offizielle <a href="https://alpinelinux.org/downloads/" target="_blank" rel="noopener noreferrer">Alpine-Downloadseite<i class="fas fa-external-link-square-alt ms-1"></i></a> verfügbar ist. Wir gehen davon aus, dass eine installierte und funktionsfähige Alpine-Version bereits vorhanden ist.</p>
<p>Um Alpine aktuell zu halten und um es zu aktualisieren empfiehlt es sich ein update durchzuführen.</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-sh" data-lang="sh"><span class="line"><span class="cl">apk update
</span></span><span class="line"><span class="cl">apk upgrade
</span></span></code></pre></td></tr></table>
</div>
</div><p>Ähnlich wie bei anderen Distributionen aktualisiert <code>apk update</code> die Paketlisten und <code>apk upgrade</code> die Packages anhand der Packetlisten.</p>

<h3 id="erstellen-der-ordnerstruktur" data-numberify>Erstellen der Ordnerstruktur<a class="anchor ms-1" href="#erstellen-der-ordnerstruktur"></a></h3>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">mkdir -p /home/data
</span></span><span class="line"><span class="cl"><span class="nb">cd</span> /var/www
</span></span><span class="line"><span class="cl">mkdir -p nextcloud.erik-skopp.de
</span></span><span class="line"><span class="cl"><span class="nb">cd</span> ~
</span></span><span class="line"><span class="cl"><span class="nb">cd</span> nextcloud.erik-skopp.de/
</span></span></code></pre></td></tr></table>
</div>
</div><p>Für die Nextcloud-Installation benötigen wir lediglich zwei Verzeichnisse: <sup id="fnref:2"><a href="#fn:2" class="footnote-ref" role="doc-noteref">2</a></sup></p>
<ol>
<li><code>/home/data</code> – Dieses Verzeichnis dient als Speicherort für die Nutzerdaten. Durch die Trennung von Anwendungs- und Nutzerdaten wird die Verwaltung von Zugriffsrechten vereinfacht und die Datensicherheit erhöht.</li>
<li><code>/var/www/nextcloud.erik-skopp.de</code> – Hier wird die Nextcloud-Installation selbst abgelegt. Dieses Verzeichnis enthält alle erforderlichen Dateien für den Betrieb von Nextcloud.</li>
</ol>
<p>Diese klare Trennung sorgt für eine bessere Organisation und erleichtert zukünftige Wartungsarbeiten.</p>

<h3 id="herunterladen-von-nextcloud" data-numberify>Herunterladen von Nextcloud<a class="anchor ms-1" href="#herunterladen-von-nextcloud"></a></h3>
<p>Zunächst laden wir die Datei <code>latest.zip</code> herunter und entpacken sie in das vorgesehene Verzeichnis. Alpine bietet in der Dokumentation ein <code>nextcloud-initscript</code><sup id="fnref:3"><a href="#fn:3" class="footnote-ref" role="doc-noteref">3</a></sup>, das diesen Prozess automatisiert. Im Wesentlichen führt das Skript dieselben Schritte aus, mit dem Unterschied, dass es zusätzlich die erforderlichen PHP-Abhängigkeiten installiert.</p>
<p>Da wir das System manuell einrichten, müssen wir die benötigten PHP-Pakete selbst installieren, um sicherzustellen, dass Nextcloud reibungslos funktioniert.</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span><span class="lnt">6
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="nb">cd</span> /var/www/nextcloud.erik-skopp.de
</span></span><span class="line"><span class="cl">wget https://download.nextcloud.com/server/releases/latest.zip
</span></span><span class="line"><span class="cl">unzip latest.zip
</span></span><span class="line"><span class="cl">mv nextcloud/* .
</span></span><span class="line"><span class="cl">rm -rf nextcloud/
</span></span><span class="line"><span class="cl">rm latest.zip
</span></span></code></pre></td></tr></table>
</div>
</div><p>Mit wget laden wir das ZIP-Archiv von Nextcloud herunter. Es ist wichtig, die Version latest zu verwenden, da nur so die neuesten Sicherheitsupdates und Funktionen enthalten sind.</p>
<p>Anschließend entpacken wir das ZIP-Archiv, wodurch die Dateien in einem zusätzlichen Unterordner nextcloud abgelegt werden. Da dieser zusätzliche Ordner nicht benötigt wird, verschieben wir den Inhalt direkt in unseren Zielordner nextcloud.erik-skopp.de. Abschließend löschen wir das ursprüngliche latest.zip-Archiv sowie den nun leeren nextcloud-Ordner.</p>
<p>Nun sollte das ganze in etwa so aussehen:</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">localhost:/var/www/nextcloud.erik-skopp.de# ls
</span></span><span class="line"><span class="cl">3rdparty           apps               console.php        index.html         ocs                public.php         status.php
</span></span><span class="line"><span class="cl">AUTHORS            composer.json      core               index.php          ocs-provider       remote.php         themes
</span></span><span class="line"><span class="cl">COPYING            composer.lock      cron.php           lib                package-lock.json  resources          updater
</span></span><span class="line"><span class="cl">LICENSES           config             dist               occ                package.json       robots.txt         version.php
</span></span></code></pre></td></tr></table>
</div>
</div>
<h3 id="installation-von-php-83" data-numberify>Installation von PHP 8.3<a class="anchor ms-1" href="#installation-von-php-83"></a></h3>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span><span class="lnt">6
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">apk add php83 php83-fpm php83-mysqli php83-json php83-openssl <span class="se">\
</span></span></span><span class="line"><span class="cl"><span class="se"></span>    php83-curl php83-gd php83-intl php83-mbstring php83-xml <span class="se">\
</span></span></span><span class="line"><span class="cl"><span class="se"></span>    php83-zip php83-bcmath php83-gmp php83-exif php83-fileinfo <span class="se">\
</span></span></span><span class="line"><span class="cl"><span class="se"></span>    php83-pcntl php83-posix php83-session php83-simplexml <span class="se">\
</span></span></span><span class="line"><span class="cl"><span class="se"></span>    php83-tokenizer php83-iconv php83-dom php83-xmlreader <span class="se">\
</span></span></span><span class="line"><span class="cl"><span class="se"></span>    php83-xmlwriter php83-pdo php83-pdo_mysql php83-opcache
</span></span></code></pre></td></tr></table>
</div>
</div><p>Um PHP allein nutzen zu können, reicht es aus, den Befehl <code>apk add php83 php83-fpm</code> auszuführen. Nextcloud erfordert jedoch zusätzliche PHP-Erweiterungen für seine Kernfunktionen und Plugins. Um nicht jede Erweiterung einzeln zu installieren, kann man mit diesem Befehl die wichtigsten Extensions direkt hinzufügen.</p>

<h3 id="installation-von-caddy" data-numberify>Installation von Caddy<a class="anchor ms-1" href="#installation-von-caddy"></a></h3>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">apk add caddy
</span></span></code></pre></td></tr></table>
</div>
</div><p>Caddy ist ein moderner, leistungsstarker Webserver und Reverse Proxy, der sich durch einfache Konfiguration, automatische HTTPS-Unterstützung und geringe Systemanforderungen auszeichnet. Er ist in Go geschrieben und wird oft als Alternative zu Nginx oder Apache verwendet.</p>
<p>Wie alle Webserver basiert Caddy auf Konfigurationsdateien, die nun bearbeitet werden müssen. Ein wesentlicher Unterschied zu Apache und Nginx besteht darin, dass Caddy die Verwaltung von SSL/TLS-Zertifikaten automatisch übernimmt. <sup id="fnref:4"><a href="#fn:4" class="footnote-ref" role="doc-noteref">4</a></sup></p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">rc-service caddy start
</span></span></code></pre></td></tr></table>
</div>
</div><p>In Alpine müssen alle sockets und Dienste manuell gestartet werden. Man kann das einfach mit <code>systemctl caddy start</code> aus Debian 12 vergleichen. Ohne den Start ist Caddy nicht nutzbasr. Debian nutzt systemd und Alpine nutzt OpenRC.</p>

<h3 id="einrichten-von-caddy" data-numberify>Einrichten von Caddy<a class="anchor ms-1" href="#einrichten-von-caddy"></a></h3>
<p>Die Konfigurationsdatei von Caddy befindet sich unter <code>/etc/caddy/Caddyfile</code> und kann als normale Textdatei bearbeitet werden. Dafür eignet sich nano, alternativ kann auch vim oder ein anderer Editor verwendet werden.</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">nano /etc/caddy/Caddyfile
</span></span></code></pre></td></tr></table>
</div>
</div><p>Diese <code>Caddyfile</code> definiert die Konfiguration für die Domain <code>nextcloud.erik-skopp.de</code> und stellt sicher, dass Nextcloud über Caddy als Webserver korrekt betrieben wird.</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-Caddy" data-lang="Caddy"><span class="line"><span class="cl"><span class="c1"># Caddy&#39;s configuration file
</span></span></span><span class="line"><span class="cl"><span class="c1"># see: https://caddyserver.com/docs/caddyfile
</span></span></span><span class="line"><span class="cl"><span class="c1"></span>
</span></span><span class="line"><span class="cl"><span class="gh">nextcloud.erik-skopp.de</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">root</span> <span class="nd">*</span> <span class="s">/var/www/nextcloud.erik-skopp.de</span>
</span></span><span class="line"><span class="cl">        <span class="k">file_server</span>
</span></span><span class="line"><span class="cl">        <span class="k">php_fastcgi</span> <span class="s">unix//run/php-fpm83/php-fpm.sock</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="k">header</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">                <span class="k">Strict-Transport-Security</span> <span class="s2">&#34;max-age=31536000; includeSubDomains; preload&#34;</span>
</span></span><span class="line"><span class="cl">                <span class="k">Referrer-Policy</span> <span class="s2">&#34;no-referrer&#34;</span>
</span></span><span class="line"><span class="cl">                <span class="k">X-Content-Type-Options</span> <span class="s2">&#34;nosniff&#34;</span>
</span></span><span class="line"><span class="cl">                <span class="k">X-Frame-Options</span> <span class="s2">&#34;SAMEORIGIN&#34;</span>
</span></span><span class="line"><span class="cl">                <span class="k">X-XSS-Protection</span> <span class="s2">&#34;1; mode=block&#34;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></td></tr></table>
</div>
</div><p>Diese Caddyfile definiert die Konfiguration für die Domain <code>nextcloud.erik-skopp.de</code> und stellt sicher, dass Nextcloud über Caddy als Webserver korrekt betrieben wird.</p>
<p>Zunächst wird die Domain <code>nextcloud.erik-skopp.de</code> als Hostname festgelegt, sodass Caddy Anfragen für diese Domain verarbeitet. Danach wird das <code>Root-Verzeichnis</code> für Nextcloud auf <code>/var/www/nextcloud.erik-skopp.de</code> gesetzt, wodurch alle statischen Dateien und PHP-Skripte von dort geladen werden. Die Zeile <code>file_server</code> aktiviert den Dateiserver von Caddy, sodass statische Inhalte wie Bilder, CSS und JavaScript direkt ausgeliefert werden können.</p>
<p>Für die PHP-Verarbeitung wird <code>php_fastcgi unix//run/php-fpm83/php-fpm.sock</code> verwendet. Dadurch werden PHP-Anfragen an einen PHP-FPM 8.3 Socket weitergeleitet, was für die korrekte Ausführung von Nextcloud erforderlich ist.</p>
<p>Zusätzlich werden mehrere <code>Sicherheits-Header</code> gesetzt, um die Nextcloud-Instanz besser abzusichern. <code>Strict-Transport-Security</code> (HSTS) sorgt dafür, dass HTTPS für ein Jahr erzwungen wird und auch Subdomains betrifft. <code>Referrer-Policy</code> verhindert, dass der Browser Referrer-Informationen an externe Seiten sendet. <code>X-Content-Type-Options</code> unterbindet das MIME-Type-Sniffing und erschwert so Angriffe. <code>X-Frame-Options</code> schützt vor Clickjacking, indem das Einbetten der Seite in Frames blockiert wird. Schließlich aktiviert <code>X-XSS-Protection</code> den XSS-Schutz im Browser.</p>
<p>Diese <code>Caddyfile</code> ermöglicht eine sichere und optimierte Bereitstellung von Nextcloud auf der Domain <code>nextcloud.erik-skopp.de</code>. Caddy übernimmt die Verwaltung von <code>statischen Dateien</code>, <code>PHP-Verarbeitung</code> und <code>Sicherheitsmechanismen</code>, wodurch eine stabile und geschützte Umgebung für Nextcloud gewährleistet wird.</p>
<p>Wenn die Config fertig ist, kann man sie formatieren. Dies sollte man tun, da sonst OpenRC sich beschwert.</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">caddy fmt --overwrite /etc/caddy/Caddyfile
</span></span></code></pre></td></tr></table>
</div>
</div><p>Anschließend muss Caddy neu gestartet werden. Dies ist wichtig, damit er die Config neu einliest.</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">rc-service caddy restart
</span></span></code></pre></td></tr></table>
</div>
</div>
<h3 id="aktivieren-von-php83" data-numberify>Aktivieren von PHP8.3<a class="anchor ms-1" href="#aktivieren-von-php83"></a></h3>
<p>Das starten von PHP8.3 hat sich immer als etwas schwierig herausgestellt.</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">rc-update add php-fpm83 default
</span></span><span class="line"><span class="cl">rc-service php-fpm83 restart
</span></span></code></pre></td></tr></table>
</div>
</div><p>Jetzt geht es um die PHP config</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">nano /etc/php83/php-fpm.d/www.conf
</span></span></code></pre></td></tr></table>
</div>
</div><p>Anschließend sind einige Anpassungen in der Datei <code>/etc/php83/php-fpm.d/www.conf</code> erforderlich. Ich empfehle, die Datei vollständig zu löschen, da nur wenige Konfigurationen benötigt werden. Diese werde ich nun im Detail erläutern.</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span><span class="lnt">19
</span><span class="lnt">20
</span><span class="lnt">21
</span><span class="lnt">22
</span><span class="lnt">23
</span><span class="lnt">24
</span><span class="lnt">25
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="o">[</span>www<span class="o">]</span>
</span></span><span class="line"><span class="cl"><span class="p">;</span><span class="nv">listen</span> <span class="o">=</span> 127.0.0.1:9000
</span></span><span class="line"><span class="cl"><span class="p">;</span><span class="nv">user</span> <span class="o">=</span> nobody
</span></span><span class="line"><span class="cl"><span class="p">;</span><span class="c1">#group = nobody</span>
</span></span><span class="line"><span class="cl"><span class="p">;</span><span class="c1">#listen.owner = nobody</span>
</span></span><span class="line"><span class="cl"><span class="p">;</span>listen.group <span class="o">=</span> nobody
</span></span><span class="line"><span class="cl"><span class="p">;</span>listen.mode <span class="o">=</span> <span class="m">0666</span>
</span></span><span class="line"><span class="cl"><span class="p">;</span><span class="nv">pm</span> <span class="o">=</span> dynamic
</span></span><span class="line"><span class="cl"><span class="p">;</span>pm.max_children <span class="o">=</span> <span class="m">50</span>
</span></span><span class="line"><span class="cl"><span class="p">;</span>pm.start_servers <span class="o">=</span> <span class="m">5</span>
</span></span><span class="line"><span class="cl"><span class="p">;</span>pm.min_spare_servers <span class="o">=</span> <span class="m">5</span>
</span></span><span class="line"><span class="cl"><span class="p">;</span>pm.max_spare_servers <span class="o">=</span> <span class="m">35</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="nv">user</span> <span class="o">=</span> caddy
</span></span><span class="line"><span class="cl"><span class="nv">group</span> <span class="o">=</span> caddy
</span></span><span class="line"><span class="cl"><span class="nv">listen</span> <span class="o">=</span> /run/php-fpm83/php-fpm.sock
</span></span><span class="line"><span class="cl">listen.owner <span class="o">=</span> caddy
</span></span><span class="line"><span class="cl">listen.group <span class="o">=</span> caddy
</span></span><span class="line"><span class="cl">listen.mode <span class="o">=</span> <span class="m">0660</span>
</span></span><span class="line"><span class="cl"><span class="nv">pm</span> <span class="o">=</span> dynamic
</span></span><span class="line"><span class="cl">pm.max_children <span class="o">=</span> <span class="m">10</span>
</span></span><span class="line"><span class="cl">pm.start_servers <span class="o">=</span> <span class="m">2</span>
</span></span><span class="line"><span class="cl">pm.min_spare_servers <span class="o">=</span> <span class="m">1</span>
</span></span><span class="line"><span class="cl">pm.max_spare_servers <span class="o">=</span> <span class="m">5</span>
</span></span></code></pre></td></tr></table>
</div>
</div><blockquote>
<p>Ich habe die alte config oben als Kommentar in der Datei beibehalten. Das macht das Fehler debugging einfacher.
Die Konfiguration für <code>PHP-FPM 8.3</code> setzt verschiedene Parameter für den Betrieb des PHP-Prozesses unter dem Benutzer <code>caddy</code>. Die einzelnen Zeilen haben folgende Bedeutung:</p></blockquote>
<ul>
<li>
<p><code>user = caddy</code> und <code>group = caddy</code><br>
Diese Zeilen legen fest, dass <code>PHP-FPM</code> als der Benutzer <code>caddy</code> in der Gruppe <code>caddy</code> ausgeführt wird. Dies ist wichtig, um eine saubere Rechteverwaltung zu gewährleisten und sicherzustellen, dass <code>PHP</code>-Prozesse unter dem Webserver-Benutzer laufen.</p>
</li>
<li>
<p><code>listen = /run/php-fpm83/php-fpm.sock</code><br>
Diese Einstellung definiert, dass <code>PHP-FPM</code> über eine <strong>Unix-Socket-Datei</strong> (<code>/run/php-fpm83/php-fpm.sock</code>) statt über eine <strong>TCP-Adresse</strong> (<code>127.0.0.1:9000</code>) kommuniziert.<br>
Die Nutzung einer <strong>Unix-Socket-Verbindung</strong> hat mehrere Vorteile:</p>
<ul>
<li>Sie ist schneller als eine <code>TCP</code>-Verbindung, da sie ohne Netzwerk-Overhead direkt auf dem Dateisystem operiert.</li>
<li>Sie ist sicherer, da keine Netzwerkverbindung geöffnet wird, wodurch Angriffe von außen ausgeschlossen sind.</li>
<li>Sie reduziert die Ressourcennutzung, insbesondere bei lokalen Webservern wie <code>Caddy</code> oder <code>Nginx</code>.</li>
</ul>
</li>
<li>
<p><code>listen.owner = caddy</code> und <code>listen.group = caddy</code><br>
Diese Einstellungen definieren, dass der <code>Caddy</code>-Server vollen Zugriff auf die <code>PHP-Socket</code>-Datei hat. Ohne diese Konfiguration könnte der Webserver <code>PHP-FPM</code> möglicherweise nicht korrekt ansprechen.</p>
</li>
<li>
<p><code>listen.mode = 0660</code><br>
Hier wird festgelegt, dass die erstellte <code>Socket-Datei</code> (<code>php-fpm.sock</code>) mit den <strong>Berechtigungen 0660</strong> versehen wird.<br>
Das bedeutet:</p>
<ul>
<li><strong>Eigentümer (<code>caddy</code>)</strong>: Lesen &amp; Schreiben (<code>6</code>)</li>
<li><strong>Gruppe (<code>caddy</code>)</strong>: Lesen &amp; Schreiben (<code>6</code>)</li>
<li><strong>Andere Benutzer</strong>: Kein Zugriff (<code>0</code>)<br>
Dadurch wird sichergestellt, dass nur der <code>Webserver-Prozess</code> auf den <code>Socket</code> zugreifen kann.</li>
</ul>
</li>
<li>
<p><code>pm = dynamic</code><br>
Diese Einstellung konfiguriert das <strong>Prozessmanagement</strong> für <code>PHP-FPM</code> auf <code>dynamisch</code>. Das bedeutet, dass <code>PHP-FPM</code> je nach Last die Anzahl der Prozesse anpasst.</p>
</li>
<li>
<p><code>pm.max_children = 10</code><br>
Dies gibt die maximale Anzahl an <code>PHP-Prozessen</code> an, die <code>PHP-FPM</code> gleichzeitig starten kann. Damit wird verhindert, dass das System überlastet wird.</p>
</li>
<li>
<p><code>pm.start_servers = 2</code><br>
Diese Zeile gibt an, dass <code>PHP-FPM</code> direkt beim Start <strong>zwei Prozesse</strong> erzeugt.</p>
</li>
<li>
<p><code>pm.min_spare_servers = 1</code><br>
Dies stellt sicher, dass mindestens <strong>ein Prozess</strong> im Leerlauf bleibt, um Anfragen sofort bearbeiten zu können.</p>
</li>
<li>
<p><code>pm.max_spare_servers = 5</code><br>
Diese Einstellung gibt an, dass maximal <strong>fünf Prozesse</strong> im Leerlauf sein dürfen, um Ressourcen zu sparen.</p>
</li>
</ul>
<p>Standardmäßig lauscht <code>PHP-FPM</code> auf <code>127.0.0.1:9000</code>, einer <code>TCP</code>-Verbindung.<br>
Die Umstellung auf eine <strong>Unix-Socket-Datei</strong> (<code>/run/php-fpm83/php-fpm.sock</code>) wurde vorgenommen, weil:</p>
<ol>
<li><strong>Leistungssteigerung</strong>: Eine <code>Unix-Socket</code> ist schneller als <code>TCP</code>, da kein Netzwerk-Stack verwendet wird.</li>
<li><strong>Sicherheit</strong>: Eine <code>TCP-Verbindung</code> könnte theoretisch von anderen Diensten oder Benutzern im Netzwerk abgefangen werden.</li>
<li><strong>Ressourcenschonung</strong>: Ein <code>Unix-Socket</code> verbraucht weniger Systemressourcen als eine <code>TCP-Verbindung</code>.</li>
<li><strong>Bessere Integration mit Caddy</strong>: Da <code>Caddy</code> lokal läuft, kann es direkt auf den <code>Unix-Socket</code> zugreifen, ohne dass ein Netzwerkport geöffnet sein muss.</li>
</ol>
<p>Durch diese Änderungen wird <code>PHP-FPM</code> effizienter, sicherer und performanter in die <code>Webserver-Umgebung</code> integriert.</p>

<h2 id="sonstiges" data-numberify>Sonstiges<a class="anchor ms-1" href="#sonstiges"></a></h2>

<h3 id="titelbild" data-numberify>Titelbild<a class="anchor ms-1" href="#titelbild"></a></h3>
<p>Das Titelbild stammt von Pixabay.<sup id="fnref:5"><a href="#fn:5" class="footnote-ref" role="doc-noteref">5</a></sup></p>
<div class="footnotes" role="doc-endnotes">
<hr>
<ol>
<li id="fn:1">
<p><a href="https://de.wikipedia.org/wiki/Dig_%28Software%29" target="_blank" rel="noopener noreferrer">Wikipedia Dig Software<i class="fas fa-external-link-square-alt ms-1"></i></a>&#160;<a href="#fnref:1" class="footnote-backref" role="doc-backlink">&#x21a9;&#xfe0e;</a></p>
</li>
<li id="fn:2">
<p>Diese Trennung habe ich mir vor etlichen Jahren abgeschaut und finde sie bis heute sinnvoll. Sie sorgt für eine klare Struktur, erleichtert die Verwaltung von Zugriffsrechten und verbessert die Wartbarkeit des Systems. Während sich <code>/home/data</code> ausschließlich um die Speicherung der Nutzerdaten kümmert, bleibt <code>/var/www/nextcloud.erik-skopp.de</code> auf die Anwendung selbst beschränkt. Dadurch lassen sich Backups gezielter anfertigen und Berechtigungen besser steuern.
<a href="https://apfelcast.com/nextcloud-28-hub-7-installation-einfache-anleitung-auf-linux-server-inkl-domain-ssl/" target="_blank" rel="noopener noreferrer">Apfelcast - Nextcloud und Nginx<i class="fas fa-external-link-square-alt ms-1"></i></a>&#160;<a href="#fnref:2" class="footnote-backref" role="doc-backlink">&#x21a9;&#xfe0e;</a></p>
</li>
<li id="fn:3">
<p><a href="https://wiki.alpinelinux.org/wiki/Nextcloud#Webserver" target="_blank" rel="noopener noreferrer">Nextcloud im Alpine Wiki<i class="fas fa-external-link-square-alt ms-1"></i></a>&#160;<a href="#fnref:3" class="footnote-backref" role="doc-backlink">&#x21a9;&#xfe0e;</a></p>
</li>
<li id="fn:4">
<p><a href="https://wiki.alpinelinux.org/wiki/Caddy" target="_blank" rel="noopener noreferrer">Caddy im Alpine Wiki<i class="fas fa-external-link-square-alt ms-1"></i></a>&#160;<a href="#fnref:4" class="footnote-backref" role="doc-backlink">&#x21a9;&#xfe0e;</a></p>
</li>
<li id="fn:5">
<p><a href="https://pixabay.com/de/illustrations/ai-generiert-cloud-computing-bergbau-8533603/" target="_blank" rel="noopener noreferrer">Quelle: AI-generiert Cloud Computing Mining auf Pixabay<i class="fas fa-external-link-square-alt ms-1"></i></a>&#160;<a href="#fnref:5" class="footnote-backref" role="doc-backlink">&#x21a9;&#xfe0e;</a></p>
</li>
</ol>
</div>
]]></content:encoded><enclosure url="https://backup.erik-skopp.de/nextcloud-auf-alpine-mit-caddy/featured-sample.webp" length="176444" type="image/webp"/></item></channel></rss>